name: NEO-P2P CI

on:
  push:
    branches: [main, develop, v0.1.1, v0.1.4]
  pull_request:
    branches: [main]

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    name: Build + Unit Tests + Lint
    runs-on: ubuntu-latest
    timeout-minutes: 30

    defaults:
      run:
        working-directory: android

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: 21
          distribution: temurin

      - name: Network invariant gate
        working-directory: .
        run: .github/scripts/check-network-invariant.sh

      - name: Locate preinstalled Android SDK
        id: android_sdk
        working-directory: .
        run: |
          SDK="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}"
          if [ -n "$SDK" ] && ls "$SDK"/cmdline-tools/*/bin/sdkmanager >/dev/null 2>&1; then
            echo "present=true" >> "$GITHUB_OUTPUT"
          else
            echo "present=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Set up Android SDK
        if: steps.android_sdk.outputs.present == 'false'
        uses: android-actions/setup-android@v3

      # tor-android 0.4.9.12 forces compileSdkMinor = 1 (its AAR metadata
      # declares minCompileMinorSdk=1), so the build needs SDK Platform 37.1.
      # The hosted runner image ships android-37 (minor 0) only.
      - name: Install Android SDK Platform 37.1
        working-directory: .
        run: |
          set -euo pipefail
          SDK="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}"
          SDKMANAGER="$(ls "$SDK"/cmdline-tools/*/bin/sdkmanager 2>/dev/null | sort -V | tail -1 || true)"
          if [ -z "$SDKMANAGER" ]; then
            echo "sdkmanager not found under '$SDK'" >&2
            exit 1
          fi
          yes | "$SDKMANAGER" --sdk_root="$SDK" --licenses >/dev/null || true
          yes | "$SDKMANAGER" --sdk_root="$SDK" "platforms;android-37.1"
          "$SDKMANAGER" --sdk_root="$SDK" --list_installed | grep -F "platforms;android-37.1"

      # F1 (2026-09-23): the pinned RNS/LXMF fork artifacts are checked into
      # android/thirdparty-repo and resolved from there — no mavenLocal, no
      # fork build, no LAN-only remote. A clean checkout builds unaided.

      - name: Set up Gradle cache
        uses: actions/cache@v4
        with:
          path: |
            ~/.gradle/caches
            ~/.gradle/wrapper
          key: ${{ runner.os }}-gradle-${{ hashFiles('**/*.gradle*', '**/libs.versions.toml') }}
          restore-keys: |
            ${{ runner.os }}-gradle-

      - name: Grant execute permission for gradlew
        run: chmod +x gradlew

      - name: Run unit tests
        run: ./gradlew :app:testDebugUnitTest :core:test --stacktrace

      - name: Lint check
        run: ./gradlew :app:lintDebug --stacktrace

      - name: Build debug APK
        run: ./gradlew :app:assembleDebug --stacktrace

      # P5/P6.2: architectural gate — only AppModule/ChainMonitor may construct
      # an HTTP client, so every network call goes through the hardened,
      # failover-aware chain monitor.
      - name: Architectural gate (single HTTP chokepoint)
        run: ../.github/scripts/check-http-chokepoint.sh

      # P5: assert the built APK is 16 KB page aligned (SQLCipher + all native
      # libs). Skipped only when zipalign is unavailable on the runner.
      - name: 16 KB page-alignment check
        run: |
          set -e
          APK="$(ls app/build/outputs/apk/debug/*.apk | head -1)"
          SDK="${ANDROID_HOME:-${ANDROID_SDK_ROOT:-}}"
          ZA=""
          if [ -n "$SDK" ]; then
            ZA="$(ls "$SDK"/build-tools/*/zipalign 2>/dev/null | sort -V | tail -1 || true)"
          fi
          if [ -z "$ZA" ]; then
            echo "zipalign not found; skipping 16 KB check"
            exit 0
          fi
          "$ZA" -c -P 16 -v 4 "$APK" >/dev/null
          echo "16 KB aligned: $APK"


      - name: Upload test results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: test-results
          path: android/app/build/reports/tests/

      - name: Upload lint results
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: lint-results
          path: android/app/build/reports/lint-results-*.html

      - name: Upload APK
        if: success()
        uses: actions/upload-artifact@v4
        with:
          name: debug-apk
          path: android/app/build/outputs/apk/debug/*.apk

  security-audit:
    name: Dependency Scanning
    runs-on: ubuntu-latest
    timeout-minutes: 10

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Check dependencies for known vulnerabilities
        uses: dependency-check/Dependency-Check_Action@main
        with:
          project: NEO-P2P
          path: android
          format: HTML
          fail_on_cvss: 9
        continue-on-error: true
