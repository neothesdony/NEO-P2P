# CRITICAL — Known Structural Issues in NEO-P2P

> **TL;DR (2026-05-30, historical):** We generated one Ed25519 key in StrongBox and then lied to five
> different protocols about what kind of key it was. **That failure is fixed** — see *Current state* below.
> The tables further down are kept as the audit record; statuses are re-verified against the current build.

Generated by Prism Full (adversarial multi-pass structural audit) on 2026-05-30.
**Last re-verified against the current build: 2026-09-14.**

Read this before touching identity, crypto, escrow, or reputation code.

---

## Current state (verified 2026-09-14 against HEAD)

| Fact | Value | Source |
|---|---|---|
| Commit | `bf17c6b` — docs(critical): re-verify the structural audit against v0.1.0-beta-6 (2026-09-14); code baseline `49700b5` (this audit commit is docs-only) | `git log -1` |
| Version | `versionCode 7` / `versionName 0.1.0-beta-6` | `app/build.gradle.kts:35-36` |
| Network | `NETWORK = "testnet"` — hardcoded, both APK variants share `com.neop2p.app.debug` | `app/build.gradle.kts:75` |
| Room schema | **v28** | `AppDatabase.kt:41` |
| Toolchain | AGP 9.3.0, Kotlin 2.3.0, Gradle 9.5.0, JDK 21, Compose BOM 2026.03.00 | `gradle/libs.versions.toml` |
| Crypto deps | bitcoinj **0.17.1**, sqlcipher-android 4.17.0, bcprov-jdk18on 1.78.1 | `gradle/libs.versions.toml` |
| UI | **17** Compose screens (`ui/screens/**/*Screen.kt`) | `find` |
| Tests | 114 files under `app/src/test` (plain JUnit 4, no Robolectric) | `find` |
| Transport | RNS/LXMF **only** — `relay1.custom-minipc.com:42420` | `NeoP2PConfig.kt` |

**The single-key identity failure is resolved.** Identity is BIP-39 (12 words) → BIP-32/SLIP-10 seed
derivation, so every protocol gets a key of the type it actually requires and there is no
cross-protocol linkability from a shared key. Verified derivation map:

```
BIP-39 mnemonic (12 words) → seed  (AES-256-GCM at rest, KEK in Android KeyStore)
  ├── m/44'/0'/0'/0/0      → Bitcoin wallet        (secp256k1, BIP-44; LEGACY + bech32 share this key)
  ├── m/44'/888'/0'/0/0    → libp2p / peerId       (Ed25519, SLIP-10) — signs `neop2p-binding-v1`
  ├── m/44'/999'/0'/0/0    → chat E2EE             (X25519, SLIP-10)  — PATH_SIGNAL
  ├── m/44'/999'/0'/0/1    → RNS identity, part 1  (X25519, SLIP-10)
  ├── m/44'/999'/0'/0/2    → RNS identity, part 2  (Ed25519, SLIP-10) — 64 B total
  └── m/44'/999'/0'/1/0    → arbitrator            (secp256k1, admin mnemonic only)
```

Sources: `IdentityManager.kt:46-56, 341-354`, `KeyDerivation.kt:111-118`, `PeerBinding.kt:15`.

The three transport legs the original audit broke are **gone entirely** (Phase 4, 2026-08-31):
`NostrClient.kt`, `LibP2PManager.kt`, `WebRTCManager.kt` do not exist in the tree. RNS + LXMF is the
only transport, and libsignal was replaced by a custom NIP-44-*inspired* scheme
(`SignalProtocol.kt:24-44`: X25519 + HKDF-SHA256 + ChaCha20-Poly1305, 12-byte nonce, peer keys in the
SQLCipher `conversation_keys` table).

---

## The Single Point of Total Failure (2026-05-30 — RESOLVED)

NEO-P2P's identity layer *was* cryptographically invalid: a single Ed25519 KeyStore keypair was the
source of every protocol identity via SHA-256.

| Protocol | Required Key Type | What We Gave It | Works? |
|----------|------------------|-----------------|--------|
| Nostr (NIP-01) | secp256k1 Schnorr | SHA-256(Ed25519 pubkey) | **No** |
| libp2p PeerID | Ed25519 or secp256k1 | SHA-256(Ed25519 pubkey) as PeerID string | **Fake PeerID** |
| Signal Protocol | Curve25519 (X25519) | Separate in-memory key (lost on restart) | **Yes, but not persisted** |

**Was:** Nostr events unsigned garbage; libp2p PeerIDs synthetically generated with no cryptographic
binding to the key; Signal sessions evaporated on restart; all identities correlatable by anyone
holding both the npub and the PeerID.

**Now:** each row is resolved — see *Current state*. The correlated-identity privacy issue is gone
because the keys no longer share a source. libp2p is no longer a transport, but the `m/44'/888'` Ed25519
key still defines the stable peerId and signs the `neop2p-binding-v1|peerId|rnsIdentityHash|destHash`
announce, so the peerId is cryptographically bound to the identity rather than synthesized.

---

## Critical Findings (Blocks v1)

| # | Location | What Broke | Severity | Fix Type | Effort | Status |
|---|----------|-------------|----------|----------|--------|--------|
| ~~1~~ | ~~`IdentityManager` entire file~~ | ~~Ed25519 cannot serve as Nostr/LN/libp2p identity. Cross-protocol linkability.~~ | ~~CRITICAL~~ | ~~Architectural rewrite~~ | ~~3-4 days~~ | ✅ FIXED (BIP-39 + BIP-32, secp256k1-kmp + Bouncy Castle deps added) |
| ~~2~~ | ~~`NostrClient.buildSignedEvent()` line 239-240~~ | ~~`"placeholder_id"` + `"placeholder_sig"` — no Schnorr signing. Relays reject all events.~~ | ~~CRITICAL~~ | ~~Add secp256k1 + NIP-01 signing~~ | ~~1 day~~ | ✅ FIXED, then MOOT — Schnorr signing landed, and Nostr itself was removed 2026-08-31 |
| ~~3~~ | ~~`AppDatabase.kt` line 29~~ | ~~SQLCipher passphrase `"neop2p_local_encryption_key_v1"` is a compile-time constant~~ | ~~HIGH~~ | ~~Derive from KeyStore via HKDF~~ | ~~30 min~~ | ✅ FIXED (`SqlCipherPassphraseManager` uses a KeyStore-wrapped AES key) |
| ~~4~~ | ~~`NeoP2PConfig.kt` line 42~~ | ~~TURN credentials `changeme` in public source. Anyone can use your TURN server.~~ | ~~HIGH~~ | ~~BuildConfig / env var injection~~ | ~~30 min~~ | ✅ FIXED, then MOOT — TURN/relay wiring removed in Phase 4; `local.properties` holds no secrets |
| ~~5~~ | ~~`SignalProtocol.kt` lines 49-51~~ | ~~All 4 Signal stores are in-memory. PreKeys, sessions, identity keys lost on restart.~~ | ~~HIGH~~ | ~~Persist to Room + SQLCipher~~ | ~~1 day~~ | ✅ FIXED (libsignal removed; peer keys persist in SQLCipher `conversation_keys`) |
| ~~6~~ | ~~`EscrowService.kt` lines 146-147~~ | ~~`"BUYER_SIG_PLACEHOLDER"` / `"SELLER_SIG_PLACEHOLDER"` — no LDK, no PSBT, no on-chain escrow~~ | ~~HIGH~~ | ~~Remove claim OR integrate bitcoinj + 2-of-3 PSBT~~ | ~~2-3 days~~ | ✅ FIXED (real 2-of-3 P2SH: redeem-script signing, payout to the attested buyer address, broadcast) |
| ~~7~~ | ~~`ReputationSystem.kt` line 70~~ | ~~Signature is `"SIG_${peerId}_${timestamp}"`. Not a cryptographic signature. Never published.~~ | ~~HIGH~~ | ~~Wire to LibP2PManager + real signing~~ | ~~1 day~~ | ✅ FIXED (BIP-340 Schnorr secp256k1 via `AttestationCodec`; sender-authenticated ingest over LXMF, persisted to SQLCipher `attestations`) |

## Medium Findings (Should Fix Before v1)

| # | Location | What Breaks | Status |
|---|----------|-------------|--------|
| ~~8~~ | ~~`IdentityManager.generateSeedPhrase()` line 131-137~~ | ~~Only 256/2048 BIP-39 words, no checksum. Not compatible with any wallet.~~ | ✅ FIXED (full 2048-word BIP-39 with checksum) |
| ~~9~~ | ~~`NostrClient.connect()` lines 108-113~~ | ~~WebSocket drops silently. No reconnection. UI shows "Connected" for dead relay.~~ | ✅ FIXED, then MOOT — Nostr removed 2026-08-31; replaced by RNS self-heal + 60s re-announce |
| ~~10~~ | ~~`EscrowService` lines 35-38~~ | ~~Escrow state in `MutableStateFlow` only — not persisted to Room. App kill = state loss.~~ | ✅ FIXED (persisted to Room on every transition; `getEscrow` resume-heal re-publishes status) |
| 11 | `WebRTCManager.createPeerConnection()` line 89-92 | `PeerConnectionFactory` created fresh per connection. No factory reuse. | ✅ CLOSED — `WebRTCManager.kt` no longer exists (WebRTC removed in Phase 4, 2026-08-31) |

## Low Priority (v1.1+)

| # | Location | What Breaks | Status |
|---|----------|-------------|--------|
| 12 | Root `build.gradle.kts` | Root build script is intentionally minimal; Android config lives in `:android` submodule. | ✅ UPDATED (README project structure reflects this) |
| 13 | `README.md` | Claims "Tor support — optional routing through Tor." Zero code exists. | ✅ UPDATED (README now says "planned v3.0") |
| 14 | `README.md` | Claims "PQXDH post-quantum key agreement (2026)." Code implements classic X3DH. | ✅ CLOSED — the README claim is gone; `PQXDH` now appears **only** in this file, as history. Keep it that way — the live doc is `docs/SECURITY_POSTURE.md` |

## New Critical Findings (2026-08-07)

| # | Location | What Breaks | Severity | Status |
|---|----------|-------------|----------|--------|
| 15 | `SignalProtocol.initialize()` | `libsignal-protocol-java` ships **javalite** protobuf classes which crash under the full `protobuf-java` runtime this project ships. | HIGH | ✅ RESOLVED 2026-08-24 — libsignal REMOVED; custom NIP-44-inspired E2EE (X25519 + HKDF + ChaCha20-Poly1305, Bouncy Castle) live, sessions persisted |

## New Capabilities Added

| # | Addition | File(s) |
|---|----------|---------|
| A | GitHub Actions CI (build + test + lint + artifact upload) | `.github/workflows/ci.yml` (active); `android-ci.yml` / `ios-ci.yml` are stale |
| B | Unit tests — 114 files today (reputation, escrow money-path, routing gates, E2EE wire format) | `app/src/test/**` |
| C | Test framework (JUnit 4, coroutines-test) | `libs.versions.toml`, `build.gradle.kts` |
| D | Bahasa Indonesia localization | `res/values-in/strings.xml` |
| E | secp256k1-kmp + Bouncy Castle deps | `libs.versions.toml`, `build.gradle.kts` |
| F | ~~NostrClient reconnection with exponential backoff~~ | **removed with the Nostr transport 2026-08-31** — replaced by RNS keepalive + 60s re-announce |
| G | ~~Hybrid P2P transport: libp2p direct + WebSocket relay fallback~~ | **removed 2026-08-31 (Phase 4)** — RNS + LXMF is the only transport |
| H | ~~WebSocket relay URL configurable via `local.properties`~~ | **removed 2026-08-31** — replaced by `RNS_TRANSPORT_NODE_HOST/PORT` |
| I | Seed-phrase verification step in onboarding | `OnboardingScreen.kt` |
| J | Payment-method bank details collection on Create Offer | `CreateOfferScreen.kt` |
| K | ~~Live relay connection status in Settings~~ | **removed with the Nostr transport 2026-08-31** — connection quality is now `PeerRegistry.ConnectionQuality` (6 states) |
| L | Nickname editing + persistence | `ProfileScreen.kt`, `IdentityManager.kt` |
| M | 16 KB-aligned SQLCipher (`sqlcipher-android` 4.17.0) | `libs.versions.toml`, `AppDatabase.kt` |

---

## Identity Rewrite Blueprint

See `IDENTITY_REWRITE.md` for the full architectural plan. **It is done** — the blueprint below is the
verified live map, not a proposal. (`IDENTITY_REWRITE.md` itself is gitignored and local-only.)

```
seed (BIP-39, 12 words)
  ├── m/44'/0'/0'/0/0    → btcKey        (secp256k1, BIP-44 wallet)
  ├── m/44'/888'/0'/0/0  → peerIdKey     (Ed25519, SLIP-10 — peerId + neop2p-binding-v1)
  ├── m/44'/999'/0'/0/0  → chatKey       (X25519, SLIP-10 — E2EE; legacy name PATH_SIGNAL)
  ├── m/44'/999'/0'/0/1  → rnsX25519     (SLIP-10)
  ├── m/44'/999'/0'/0/2  → rnsEd25519    (SLIP-10 — together: 64-byte RNS identity)
  └── m/44'/999'/0'/1/0  → arbitratorKey (secp256k1, admin mnemonic only)
```

Seed encrypted with KeyStore-wrapped AES-256-GCM. Derivable keys stay in memory.
Backup = BIP-39 mnemonic. Restore = validate checksum, derive all keys from path.
**The SQLCipher DB and encrypted prefs are excluded from cloud backup AND device-transfer** — the
mnemonic is the only restore path.

---

## Escrow Money-Path Audit (2026-09-13) — F-1/F-2/F-3

- **F-3 (payout poisoning):** `releaseFunds` signed and broadcast whatever `psbt_unsigned` held, and the router adopted a remote psbt with no owner guard — a peer could swap the payout tx. Now `ReleaseIntegrity.verdict` re-derives the destination from the attested buyer address (anchored in the already-funded redeem script) at broadcast, `EscrowRouter.shouldAdoptRemotePsbt` keeps the creator's tx sovereign, and a refused payout is rebuilt once (`releaseGateRecovery`), never looped.
- **F-2 (unauthenticated status):** `escrow_status` was accepted from any sender whose body claimed a party, and the seller's authoritative row could be terminated by a forged `CANCELLED`. Now `P2POrchestrator` requires a verified sender binding, `EscrowRouter.senderIsCounterparty` binds the sender to the local row, and the creator row refuses a remote `CANCELLED`.
- **F-1 (dead refund):** after C1 made the buyer/seller keys distinct, `refundInternal` still required one key to match BOTH role pubkeys — every "Cancel & Refund" and the auto-refund failed forever. The pipeline is deleted; a refund now travels through the arbitrator (`refundRequestKind` → dispute → co-signed resolution), and a stalled funded escrow escalates to a dispute instead of attempting an impossible refund.

**Live escrow constants (re-verified 2026-09-14, `EscrowService.kt:344-421` + `NeoP2PConfig.kt:61-70`):**
funding window `30 min` with a `15 min` warning; funded-stall `2 h` + `2 h` grace → `ESCALATE` to a
dispute; payment window `1 h` + `1 h` grace → auto-`DISPUTED`; `MIN_NETWORK_FEE_SATS = 250` and the
`DUST_THRESHOLD_SATS = 546` fee-output floor live in `EscrowService`; `MIN_FEE_SATS = 546` (the
offer-level dust floor) and `FEE_NUM = 5` / `FEE_DEN = 1000` live in `NeoP2PConfig` — fee =
`(sats * 5) / 1000`, seller-only.

## Security Remediation (2026-09-13) — CVE / privacy

| # | Location | What Broke | Severity | Status |
|---|----------|-----------|----------|--------|
| 16 | `libs.versions.toml` (bitcoinj) | `CVE-2026-44714` / `GHSA-hfcf-v2f8-x9pc` — bitcoinj < 0.17.1 P2PKH/P2WPKH script-verification bypass in `ScriptExecution.correctlySpends` | CRITICAL | ✅ FIXED 2026-09-13 — bumped 0.16.2 → 0.17.1 + full API migration; `BitcoinjCveRegressionTest` fails on 0.16.2 and passes on 0.17.1 |
| 17 | `data/p2p/SignalProtocol.kt` (doc) | Comment claimed XChaCha20 / 24-byte nonce; the real primitive is ChaCha20-Poly1305 / 12-byte nonce | LOW | ⚠️ MOSTLY FIXED 2026-09-13 — KDoc + wire-format assertion corrected, but the `Log.d` at `SignalProtocol.kt:105` and the `libs.versions.toml:62,73` comments still say "XChaCha20" (see item 24) |
| 18 | `res/xml/data_extraction_rules.xml` | SQLCipher DB + encrypted prefs were eligible for device-transfer (cloud backup already excluded) | MEDIUM | ✅ FIXED 2026-09-13 — `<device-transfer>` excludes added |
| 19 | Invite deep links (`neop2p://peer/<id>`) | A pasted invite carried no binding to the peer's RNS identity | MEDIUM | ✅ FIXED 2026-09-13 — `#<32-hex identityHash>` fragment + `parseInvite` validation; unverified links flagged |

---

## Open items found during the 2026-09-14 re-verification

These are **not** regressions of the original audit — they are new observations, recorded here because
this file is the place where structural problems live. None are exploitable as written.

| # | Where | Observation | Severity |
|---|-------|-------------|----------|
| 20 | `IdentityManager.kt:47, 58-62, 91, 343, 354` | Dead Nostr key material is still derived and cached: `PATH_NOSTR`, `PATH_NOSTR_TRADE_PREFIX`, `nostrKeyPair`, and `Identity.nostrPubkeyHex`/`nostrPrivateKeyHex`. No transport consumes it since 2026-08-31. Cruft, not a break — but it keeps a secp256k1 identity key alive and in memory for no reason | LOW (cleanup) |
| 21 | `EscrowService.kt:422-429`, `IdentityManager.kt:560`, `WalletService.kt:57` | `NET_PARAMS` maps non-mainnet → `TestNet3Params`, while the chain endpoints are **testnet4** (`ChainMonitor.kt:44-45`). Testnet4 kept the testnet3 address prefixes/HRP, so addresses and serialization still match — **verify before assuming full testnet4 correctness** rather than trusting this note | LOW (verify) |
| 22 | `data/p2p/SignalProtocol.kt`, `IdentityManager.PATH_SIGNAL`, `SignalSession` | Vestigial names for the custom NIP-44-inspired scheme. The crypto is correct; the name invites a future reader to assume it is Signal Protocol | LOW (naming) |
| 23 | Repo root | `CRITICAL.md` is listed in `.gitignore:59` **but is still tracked** (`git ls-files`). It is also the only place `PQXDH` appears. Decide: keep it tracked on purpose, or `git rm --cached` it with the rest of the internal docs | LOW (repo hygiene) |
| 24 | `data/p2p/SignalProtocol.kt:105`, `gradle/libs.versions.toml:62,73` | Stale "XChaCha20" labels survive the item-17 doc fix; the cipher is plain ChaCha20-Poly1305 with a 12-byte nonce. Cosmetic, but it is the same false claim item 17 closed | LOW (naming) |

---

## What's Actually Good

This audit is harsh because the bar is high. The things that work are genuinely solid:

- **Infrastructure**: RNS transport node (official Python rnsd) + LXMF propagation node on Oracle Cloud Free Tier. Deploy scripts are clean. Docker compose is well-structured.
- **UI**: 17 Compose screens with Material 3 dark theme. Navigation graph, Hilt DI, ViewModels all correct.
- **P2P transport**: RNS + LXMF — the only transport since Phase 4 (2026-08-31); libp2p/Nostr/WebRTC/ws-relay removed.
- **Identity**: BIP-39/BIP-32/SLIP-10 derivation with per-protocol key types, KeyStore-wrapped seed, and a **network-aware, integrity-checked fee wallet** — `NeoP2PConfig` embeds a signed address + pubkey + signature trio for mainnet *and* testnet and selects by `BuildConfig.NETWORK` (`FEE_WALLET_ADDRESS` is now a `val`), so a testnet build can never carry the mainnet `bc1…` fee address; both trios are asserted by `FeeWalletNetworkCompatibilityTest`.
- **Escrow**: real 2-of-3 P2SH with a pre-broadcast payout gate, role-signed destination attestations, and arbitrator co-signed refunds.
- **Project structure**: Clean separation of concerns. Domain models, repositories, use cases, data sources. Good Kotlin patterns.

---

*The original closing warning ("if Identity Issue #1 is not fixed before v1.0, the app cannot function as a
Nostr-based P2P exchange") is now obsolete twice over: the identity failure was fixed, and the app does
not use Nostr at all. The live equivalent: **any change to the derivation map in `IdentityManager` /
`KeyDerivation` is a breaking identity change** — peerIds, RNS identities, wallet addresses, E2EE keys,
and the arbitrator key all hang off those paths.*
